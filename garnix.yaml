builds:
  include:
    # Module tests (both platforms)
    - "legacyPackages.x86_64-linux.test-*"
    - "legacyPackages.aarch64-darwin.test-*"
    # Integration tests that exercise home-manager switch (Linux only — NixOS VMs)
    - "legacyPackages.x86_64-linux.integration-test-*"
    # The home-manager package itself
    - "packages.x86_64-linux.home-manager"
    - "packages.aarch64-darwin.home-manager"
  exclude: []
